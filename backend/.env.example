# ==============================================================
# Legistra Backend — Environment Variables
# ==============================================================
# Copy this file to .env and fill in with your actual values.
# NEVER commit .env to Git.
# ==============================================================

# Flask
FLASK_ENV=production
SECRET_KEY=change-me-to-a-random-64-char-string
DEBUG=False

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=eyJhbGciOi...your-anon-key

# JWT (defaults to SECRET_KEY if not set)
# JWT_SECRET_KEY=optional-separate-jwt-secret

# CORS (comma-separated origins)
# CORS_ORIGINS=http://localhost:3000,http://localhost

# File uploads
# MAX_UPLOAD_MB=50

# Redis (for Celery — optional, only needed for async analysis)
# REDIS_URL=redis://localhost:6379/0

# AI / ML (optional)
# HUGGINGFACE_API_KEY=your-key
# OPENAI_API_KEY=your-key
